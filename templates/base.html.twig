<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="{{ asset('img/glider.svg') }}">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    
    <body>
        <div class="wrap">
            <header class="site-header">
                <span class="site-title">MVC DV1608 - Andi Dupa</span>
            </header>

            <nav class="nav">
                <a class="nav-btn" href="{{ path('home') }}">Home</a>
                <a class="nav-btn" href="{{ path('about') }}">About</a>
                <a class="nav-btn" href="{{ path('lucky') }}">Lucky</a>
                <a class="nav-btn" href="{{ path('report') }}">Report</a>
                <a class="nav-btn" href="{{ path('pig_init_get') }}">Game</a>
                <a class="nav-btn" href="{{ path('api') }}">API</a>
                <a class="nav-btn" href="{{ path('card_home') }}">21 game</a>
            </nav>

            <main class="main">
                {% block body %}{% endblock %}
            </main>

            <footer class="footer">
                <div class="grid">
                    <div class="grid-child">
                        <p>
                            <a class="white" href="https://github.com/dbwebb-se/mvc/tree/main">Kursens GitHub repo</a>
                        </p>
                    </div>
                    <div class="grid-child">
                        <p>
                            Välkommen till min me-sida i MVC kursen!
                        </p>
                    </div>
                    <div class="grid-child">
                        <p>
                            <a class="white" href="https://github.com/AndiDupa/mvc-report">Mitt GitHub repo</a>
                        </p>
                    </div>
                </div>
            </footer>
        </div>
    </body>
</html>
